spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/Quwenqian/Spring-Config-Project.git
          search-paths: spring-cloud-config
